This repository contains all optimisation and simulation data produced in the DQCode project. 

> [!NOTE]
> The simulation data column `avg_fidelity` contains the average overlap of corrected state and target state instead of the average fidelity. This has been fixed in the codebase.
